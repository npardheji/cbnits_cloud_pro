policies:
- name: cloudtrail-do-not-log-data-read-events_validated
  resource: aws.cloudtrail
  comment: |
    
  filters:
    - type: event-selectors
      key: EventSelectors[].DataResources
      value: 
      op: eq