policies:
    - name: firewall_access_validated
      description: |
        GCP firewall accepts from "0.0.0.0/0" 
      resource: gcp.firewall
      # mode:
      #   type: gcp-audit
      #   methods: 
      #     - AutoTag

      filters:
        - and:
          - type: value
            key: direction
            value: INGRESS

          - type: value
            key: sourceRanges
            value: 
              - "0.0.0.0/0"
      
      # actions:
      #   - type: modify
      #     sourceRanges: 
      #         - "0.0.0.0/0"
